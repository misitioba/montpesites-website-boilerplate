# montpesites-website-boilerplate

## Installation

````js
yarn
````

## Development

````js
ms --dev --port 3000//to serve and watch for changes
ms //same as ms --dev
````

## Production

````js
ms --serve --port 3000 //to serve
ms --build //to generate static files only
````
